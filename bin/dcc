#!/bin/bash
filename=$(date +dragonboard410-dev.ccc.%Y%m%d_%H:%M:%S.$1.log)
filepath=/home/luwh/log/compile
file=$filepath/$filename
tfile=$filepath/dragonboard410-dev.ccc.last.log

~/repos/dragonboard410-dev.ccc.sh | tee $tfile
if [[ $? != 0 ]]; then
	mv $tfile $file
fi

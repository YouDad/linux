case $1 in
	b)vim ~/.vim/base_set.vim;;
	k)vim ~/.vim/key_map.vim;;
	p)vim ~/.vim/plug.vim;;
	h)
cat << _EOF_
b. ~/.vim/base_set.vim
k. ~/.vim/key_map.vim
p. ~/.vim/plug.vim
*. ~/.vimrc
_EOF_
;;
	*)vim ~/.vimrc;;
esac
